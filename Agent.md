# Agent Notes

- On request store compacted and summarized conversations under agent-docs using the current timestamp and the following format: `YYYY-MM-DD-HH:MM-short-title.md`.
- Use `cargo check` after change iteration and fix findings if there are any
